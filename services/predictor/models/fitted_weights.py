"""Ridge 회귀로 적합한 종목별 계수.

적합 방법: 과거 종가(KRX, yfinance) vs 같은 날짜 프록시 변동률(토큰가·해외
상관종목·매크로)로 회귀. 표본이 작고(토큰 상장 이후 겹치는 기간뿐) 반도체
프록시끼리 상관관계가 높아 OLS 대신 ridge(L2)를 쓰고, alpha는 LOOCV로
선택했다. 재적합할 땐 `python scripts/fit_weights.py`를 그대로 다시 돌리고
출력값을 아래 FITTED_MODELS에 수동으로 옮겨 적으면 된다 (docs/PRD.md 4.2 참고).

적합 시점: 2026-08-14 (최초 2026-08-06, ES=F/NQ=F 선물 피처 추가 + 표본
8거래일 추가 누적으로 재적합)
표본: 종목별 45일 (토큰 상장 이후 KRX·Bybit 겹치는 거래일 중 2026-07-31
극단치 1건 제외 — 아래 "주의" 참고)

2026-08-14 변경: ES=F(S&P500 선물)/NQ=F(나스닥 선물) 피처 추가. 개별주
(MU/NVDA 등)는 미국 정규장이 닫히면 다음 개장 전까지 전일 종가로
멈춰있어 KRX 장외시간엔 사실상 신호가 없었는데, 선물은 거의 24시간
거래돼(실측 확인) 그 공백을 메워준다 (docs/PRD.md 3.2엔 원래 있었는데
구현이 빠져있었음). 계수는 아직 작지만(0.002~0.015, alpha=3000 정규화로
축소) 표본이 쌓이면 커질 여지가 있음.

검증 결과 (동일 45일 표본·동일 naive 기준선으로 나란히 비교, 단위 MAE%):

| 방식                          | SAMSUNG | SKHYNIX |
|-------------------------------|---------|---------|
| naive("항상 0%")               | 5.08%   | 6.25%   |
| ridge LOOCV(진짜 out-of-sample)| 4.75%   | 5.65%   |

(참고: 2026-08-06 최초 적합 시 40일 표본 기준 naive 5.28%/6.50%,
ridge LOOCV 4.93%/5.88% — 표본이 늘면서 둘 다 소폭 개선됐지만 여전히
naive 대비 우위가 크진 않음.)

**솔직한 결론**: 손으로 찍은 가중치·ridge 둘 다 naive는 확실히 이기지만,
ridge가 손으로 찍은 가중치보다 낫다고는 말할 수 없다 (표본 40일에서
차이가 노이즈 범위 안). 그래도 ridge를 채택한 이유는 (1) 사람이 임의로
정한 숫자가 아니라 데이터로 재현 가능한 절차라 데이터가 쌓일수록
계속 재적합/개선이 가능하고, (2) LightGBM/XGBoost로 넘어갈 때 같은
데이터 파이프라인을 그대로 확장할 수 있기 때문. 현재 시점에는 "정확도
낮음, 검증 중" 상태로 취급해야 한다.

주의:
- 표본 기간에 KOSPI 사상 최대 등락(2026-07-28~31, -17%대 하락 후 +18%
  반등, 최태원 회장 장내매수·숏스퀴즈)이 있었음 — 이 날은 계수 왜곡을
  막기 위해 학습 표본에서 제외했다. 국내 수급/공매도 신호(flows.py, 아직
  스텁)가 이런 이벤트를 잡을 유일한 방법이라 다음 작업으로 예정됨.
- 표본이 40일뿐이라 통계적으로 얇다. 데이터가 쌓이는 대로(최소 3~6개월)
  재적합하고, 궁극적으로는 LightGBM/XGBoost로 교체할 것.
"""

from __future__ import annotations

FEATURES = [
    "token",
    "NVDA",
    "MU",
    "SOXX",
    "TSM",
    "SMH",
    "ES=F",
    "NQ=F",
    "KRW=X",
    "DX-Y.NYB",
    "^TNX",
    "^VIX",
    "BTC-USD",
    "ETH-USD",
]

FITTED_MODELS: dict[str, dict] = {
    "SAMSUNG": {
        "intercept": -1.1431,
        "coefficients": {
            "token": 0.0939,
            "NVDA": 0.0167,
            "MU": 0.0850,
            "SOXX": 0.0551,
            "TSM": 0.0437,
            "SMH": 0.0437,
            "ES=F": 0.0020,
            "NQ=F": 0.0101,
            "KRW=X": -0.0184,
            "DX-Y.NYB": -0.0002,
            "^TNX": -0.0120,
            "^VIX": -0.0520,
            "BTC-USD": -0.0205,
            "ETH-USD": -0.0210,
        },
        "resid_std": 5.328,
        "alpha": 3000,
        "n": 45,
    },
    "SKHYNIX": {
        "intercept": -1.4620,
        "coefficients": {
            # 2026-08-17 추가, 회귀로 적합한 값이 아니라 수동으로 크게 잡은
            # 값이다(사용자 요청 — "SKHY 값 있을 때 비중을 많이 높여").
            # 근거: SKHY는 Bybit 토큰화 선물(token, 유동성 낮은 크립토
            # 무기한선물)과 달리 나스닥 정규 상장 종목이라 가격발견/유동성
            # 신뢰도가 훨씬 높음(collectors/adr.py 참고) — 그래서 token
            # 계수(0.12)보다 한참 높게 잡았다. 아직 이 피처로 학습한 표본이
            # 없어 통계적 근거는 없으므로 "질적 판단"으로 취급할 것 — 데이터
            # 쌓이면(scripts/fit_weights.py) 반드시 재적합해서 이 값을 대체.
            "adr": 0.6,
            "token": 0.1207,
            "NVDA": 0.0160,
            "MU": 0.1264,
            "SOXX": 0.0677,
            "TSM": 0.0290,
            "SMH": 0.0531,
            "ES=F": 0.0037,
            "NQ=F": 0.0146,
            "KRW=X": -0.0199,
            "DX-Y.NYB": 0.0012,
            "^TNX": -0.0164,
            "^VIX": -0.0331,
            "BTC-USD": -0.0204,
            "ETH-USD": -0.0196,
        },
        "resid_std": 6.501,
        "alpha": 3000,
        "n": 45,
    },
}

FACTOR_LABELS: dict[str, str] = {
    "adr": "ADR(나스닥:SKHY)",
    "token": "토큰화 주식/선물(Bybit)",
    "NVDA": "Nvidia",
    "MU": "Micron",
    "SOXX": "SOXX",
    "TSM": "TSMC",
    "SMH": "SMH",
    "ES=F": "S&P500 선물",
    "NQ=F": "나스닥 선물",
    "BTC-USD": "Bitcoin",
    "ETH-USD": "Ethereum",
    "DX-Y.NYB": "DXY",
    "KRW=X": "USD/KRW",
    "^TNX": "미국 10년물",
    "^VIX": "VIX",
}
